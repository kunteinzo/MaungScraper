# Maung Scraper

&emsp;My scraping collection.

## Kotlin Gradle Project

```bash
cd JavaMgScraper
./gradlew run # and other Gradle tasks
```

## Python (venv)

```python
cd PyMgScraper

# create Py venv and install requirements if haven't create .venv yet
python3 -m venv .venv && \
source .venv/bin/activate && \
pip install -r requirements.txt

# run
python main.py # or other py script
```